import Foundation
import Swafka

public enum TestTopic: Topicable {
    case failure
    case success
}

public enum TestTopic2: Topicable {
    case failure
    case success
}

public enum TestTopic3: Topicable {
    case failure
    case success
}
